/**
 * @description Tests for the terminal module
 */

const { expect } = require('chai');

const ui = require('../src/ui');

describe('UI Tests', () => {
  it('should create a new Terminal UI instance', () => {
    // TODO
  });
});
